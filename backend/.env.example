# # Django settings
DEBUG=False
SECRET_KEY=secret_key
ALLOWED_HOSTS=anisphere.org,www.anisphere.org,localhost
SITE_URL=https://anisphere.org
CSRF_TRUSTED_ORIGINS=https://anisphere.org,https://www.anisphere.org

# MySQL Database
DB_NAME=anisphere_db
DB_USER=user_name
DB_PASSWORD=iQ8zH0uM9k
DB_HOST=localhost
DB_PORT=3306

# Email settings
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=email-host
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=email
EMAIL_HOST_PASSWORD=email-host-password
DEFAULT_FROM_EMAIL=email

# Google OAuth
GOOGLE_CLIENT_ID=id
GOOGLE_CLIENT_SECRET=secret

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0

# CORS — оба домена обязательны (фронтенд может идти с www или без)
CORS_ALLOWED_ORIGINS=https://anisphere.org,https://www.anisphere.org

DEBUG=True
SECRET_KEY=secret-key

# Database (Docker MySQL на порту 3307)
DB_NAME=anisphere_db
DB_USER=user_name
DB_PASSWORD=password
DB_HOST=localhost
DB_PORT=3307

# Redis (Docker)
REDIS_HOST=localhost
REDIS_PORT=6379

# CORS для локальной разработки
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://localhost:3000,http://127.0.0.1:3000

# Allowed hosts
ALLOWED_HOSTS=localhost,127.0.0.1